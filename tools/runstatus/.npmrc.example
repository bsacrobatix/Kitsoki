# Copy to .npmrc and add your own credentials.
# Auth belongs in your user-level ~/.npmrc, not in this repo file.

registry=https://registry.npmjs.org/
always-auth=false
@jsr:registry=https://npm.jsr.io/
