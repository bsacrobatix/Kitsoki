# Remote admission is the only writer that materializes external worker
# bundles into this queue. Do not start the consumer with an absent authority.
[Unit]
Requires=kitsoki-queue-admission.service
After=kitsoki-queue-admission.service
