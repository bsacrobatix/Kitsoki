story_dirs:
  - /opt/pog/current/stories

auth:
  mode: required
  public_url: __PUBLIC_BASE_URL__
  github:
    client_id: __GITHUB_CLIENT_ID__
    client_secret: ${KITSOKI_HOSTED_POG_GH_CLIENT_SECRET}
  admins:
    - __GITHUB_ADMIN__
  session_ttl: 168h
