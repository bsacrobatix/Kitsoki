# Validating the fix — produce the validation artifact

You are validating the applied fix for **bug1** —
*bug1 (gh-4115) — Kubernetes env-var override of a dashed gear name is silently dropped. A gear named with dashes (e.g. `static-authz-plugin`) is stored in AppConfig.gears under the dashed key. In Kubernetes, env var names cannot contain dashes (C_IDENTIFIER rule), so operators MUST use underscores: APP__GEARS__STATIC_AUTHZ_PLUGIN__CONFIG__PRIORITY=50. The figment Env::prefixed("APP__").split("__") pipeline lowercases and produces key path gears.static_authz_plugin.config.priority, but the HashMap entry is keyed `static-authz-plugin` (dashes), so the lookup misses and the override is silently ignored — the gear keeps its YAML value (e.g. priority 100) instead of the overridden 50. Expected: the underscore env-var form should override the dashed gear. Source: libs/toolkit/src/bootstrap/config/mod.rs (AppConfig::load_layered). Tests: cargo test -p cf-gears-toolkit --features bootstrap --lib bootstrap::config. A gear whose name already uses underscores is the negative control and already works.* against the full environment.

The review found:

> Reviewed the implementation in `libs/toolkit/src/bootstrap/config/mod.rs`. The fix adds post-extraction reconciliation for Kubernetes-safe underscore aliases of dashed gear names, deep-merging the alias value into the dashed gear entry while preserving explicit underscore-named gears.

Tests added/modified: `libs/toolkit/src/bootstrap/config/mod.rs` now includes `test_env_override_uses_kubernetes_safe_name_for_dashed_gear`, which reproduces the original failure with `static-authz-plugin` and `APP__GEARS__STATIC_AUTHZ_PLUGIN__CONFIG__PRIORITY=50`. It also includes an underscore-named gear control (`static_authz_control`) to confirm existing underscore behavior still works.

Tests run: `cargo test -p cf-gears-toolkit --features bootstrap --lib bootstrap::config`. The suite passed locally and matches the supplied CI log: 63 passed, 0 failed, 0 ignored, 252 filtered out. The bug reproduction now passes and no regressions appeared in the focused config test suite.

Blockers: none.

The build / deploy / validation run produced:

```
pattern ./...: directory prefix . does not contain main module or its selected dependencies

```



## Outcomes

- `pass` — the bug's reproduction now produces the expected outcome and
  no other regressions surfaced.
- `fail_short` — a minor adjustment to the implementation will fix it
  (control returns to `implementing`).
- `fail` — the fix is wrong; the proposal needs to be redrafted (control
  returns to `proposing`).
- `infra_error` — the environment was unreachable / broken in a way that
  has nothing to do with the fix.

## Output

Submit a `validate_artifact` (see `schemas/validating_artifact.json`).
`next_action_hint` is consumed by the next iteration's prompt to steer
the refinement — be specific about what should change.
