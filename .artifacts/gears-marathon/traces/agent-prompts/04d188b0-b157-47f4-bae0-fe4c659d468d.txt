# Implementing the fix — apply edits to the worktree

You are implementing the fix for **bug5** — *bug5 — resource-group wrongly forces the RG type-registry prefix onto allowed-membership type codes. In cyberware-resource-group, TypeService::create_type / update_type validate each allowed_membership_types entry with validation::validate_type_code, which enforces the RG namespace prefix (gts.cf.core.rg.type.v1~). But membership resource types are EXTERNAL domain types (e.g. gts.cf.core.idp.user.v1~) per DESIGN.md and ADR-001 Type Map; they must NOT be forced into the RG type-registry namespace. Effect: callers must wrap external types in the RG prefix purely to pass validation — a contract divergence. Expected: allowed-membership type codes accept external (non-RG-prefixed) domain types, while still rejecting empty and over-length (>1024) codes; RG-prefixed codes remain accepted for backwards-compat. add_membership/remove_membership are already correct and must stay unchanged. Source: modules/system/resource-group/resource-group/src/domain/ (validation.rs, type_service.rs). Tests: cargo test -p cyberware-resource-group --test domain_unit_test.*
inside the worktree at `/Users/brad/code/gears-rust/.worktrees/marathon-bug5-gpt`.

The proposing room produced this proposal:

> Add a separate validation helper for allowed membership type codes, for example `validation::validate_membership_type_code`, that trims/lowercases consistently and rejects only empty codes and codes longer than 1024 characters. Do not require `RG_TYPE_PREFIX` in that helper. Replace only the two `allowed_membership_types` loops in `TypeService::create_type` and `TypeService::update_type` to call the new membership helper. Leave `validate_type_code` unchanged for resource-group type codes and `allowed_parent_types`, so RG type creation, parent validation, and group service validation keep their current RG-prefix contract. Keep the existing repository `resolve_ids` checks unchanged so membership entries still have to refer to registered type rows. Cover the behavior with the focused create/update tests in `type_service_test.rs` that seed a registered external code and assert it round-trips through `allowed_membership_types`; also add or keep checks that empty and >1024 membership codes still fail.

**Root cause:** `allowed_membership_types` are membership resource type codes, but `TypeService::create_type` and `TypeService::update_type` validate them with `validation::validate_type_code`. That helper is intentionally scoped to resource-group type registry codes and enforces `RG_TYPE_PREFIX` in `modules/system/resource-group/resource-group/src/domain/validation.rs:20-44`. The offending call sites are the membership loops in `modules/system/resource-group/resource-group/src/domain/type_service.rs:75-82` and `modules/system/resource-group/resource-group/src/domain/type_service.rs:228-230`, so external registered domain types fail before the repository existence check can accept them.

**Affected files:** modules/system/resource-group/resource-group/src/domain/validation.rs, modules/system/resource-group/resource-group/src/domain/type_service.rs, modules/system/resource-group/resource-group/tests/type_service_test.rs



## What to do

Make the edits described in the proposal. You are running with cwd set to
the worktree (`/Users/brad/code/gears-rust/.worktrees/marathon-bug5-gpt`), so file paths in the proposal's
`affected_files` are valid relative paths you can hand straight to
the `Read` and `Edit` (or `Write`) tools.

You MUST actually modify the files. The downstream `testing` room will
re-run the full suite against this worktree's HEAD — if your edits don't
compile or they break other tests, the pipeline rejects the work and bounces
it back to you. Catch that **now**, before you submit.

## Verify your own work before submitting (REQUIRED)

Do not submit a fix you have not verified. You have a shell — use it. Work
this loop until it is green, then submit:

1. **Build.** Run the project's build (e.g. `go build ./...`). If it does not
   compile, fix it and rebuild. Never submit a fix that does not build.
2. **Targeted test.** Run the test(s) that exercise this bug — the
   reproduction test and the package(s) you changed
   (e.g. `go test ./path/to/changed/pkg/...` or `-run <TestName>`). The
   reproduction must now PASS where it failed before.
3. **Don't break the neighbours.** Run the broader suite for the area you
   touched (the changed packages and any package that imports them). If your
   change made a previously-passing test fail, that is a regression — fix it
   or choose a narrower edit. A fix that solves the bug but breaks other tests
   is NOT done.
4. **Iterate.** If any step fails, edit and repeat from step 1. Stay in this
   loop until build + targeted tests + neighbours are all green.

Prefer the **smallest, most local** change that fixes the root cause. A broad
edit to shared/engine internals is far more likely to break unrelated tests —
if a narrow, local fix works, take it.

## Constraints

- Stay inside the worktree. Do not edit `.git` metadata or any path
  outside `/Users/brad/code/gears-rust/.worktrees/marathon-bug5-gpt`.
- Keep the change minimal and in-scope. Touch the files in `affected_files`;
  if you genuinely need to edit something else (including to keep other tests
  green), add it to `files_changed` so the reviewer sees the scope.
- Don't run `git commit` yourself — the pipeline does that after you
  return. Leave the worktree dirty with your edits; the commit step picks up
  everything (including any test you added or repaired while verifying).
- Set `applied: false` with `blockers` ONLY if you genuinely could not reach a
  green build+test state — never submit `applied: true` for a fix you could
  not get to compile and pass.

## Output

Submit an `implementing_artifact` (see `schemas/implementing_artifact.json`).
Required fields:
- `summary_title`: one-line description of what you applied.
- `summary_markdown`: human-readable diff narrative (what changed, where,
  why each edit). This is what a human reviewer reads at the testing
  checkpoint.
- `files_changed`: every path you actually edited (or attempted to edit).
- `applied`: `true` if every required edit was made, `false` if you hit a
  blocker. When `false`, populate `blockers` with the reason.
