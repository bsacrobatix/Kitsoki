# Closing the bug-fix run — produce the done artifact

You are wrapping up the run for **bug1** —
*bug1 (gh-4115) — Kubernetes env-var override of a dashed gear name is silently dropped. A gear named with dashes (e.g. `static-authz-plugin`) is stored in AppConfig.gears under the dashed key. In Kubernetes, env var names cannot contain dashes (C_IDENTIFIER rule), so operators MUST use underscores: APP__GEARS__STATIC_AUTHZ_PLUGIN__CONFIG__PRIORITY=50. The figment Env::prefixed("APP__").split("__") pipeline lowercases and produces key path gears.static_authz_plugin.config.priority, but the HashMap entry is keyed `static-authz-plugin` (dashes), so the lookup misses and the override is silently ignored — the gear keeps its YAML value (e.g. priority 100) instead of the overridden 50. Expected: the underscore env-var form should override the dashed gear. Source: libs/toolkit/src/bootstrap/config/mod.rs (AppConfig::load_layered). Tests: cargo test -p cf-gears-toolkit --features bootstrap --lib bootstrap::config. A gear whose name already uses underscores is the negative control and already works.* after **0** refinement cycle(s).

The validation produced:

> Validated the Kubernetes-safe environment variable override fix for dashed gear names in `libs/toolkit/src/bootstrap/config/mod.rs`.

Evidence:
- Inspected `AppConfig::load_layered`; it calls `reconcile_kubernetes_safe_gear_aliases` after layered extraction and gear-file merging.
- Confirmed the reconciliation maps dashed gear names to underscore aliases, deep-merges alias values into the dashed gear entry, and skips reconciliation when an explicit underscore gear exists.
- Confirmed the regression test `test_env_override_uses_kubernetes_safe_name_for_dashed_gear` covers `static-authz-plugin` with `APP__GEARS__STATIC_AUTHZ_PLUGIN__CONFIG__PRIORITY=50` and verifies the dashed gear receives priority `50` while the underscore-named control still works.
- `cargo check -p cf-gears-toolkit --features bootstrap` passed.
- `cargo test -p cf-gears-toolkit --features bootstrap --lib bootstrap::config` passed: 63 passed, 0 failed, 0 ignored, 252 filtered out.

The previous `pattern ./...: directory prefix . does not contain main module or its selected dependencies` output is from a Go-style command and is not applicable to this Cargo workspace validation.



## Constraints

- `lessons` must be drawn from this run's actual evidence — not generic
  best-practice. At minimum one lesson per non-trivial cycle.
- Each lesson cites a category (e.g. `api-patterns`, `failure-patterns`,
  `judge-misclassification`) and a severity.
- `summary_markdown` is the postmortem: what the bug was, what the fix
  did, what cost the cycles (if any), and what changed about how we'd
  approach a similar bug next time.

## Output

Submit a `done_artifact` (see `schemas/done_artifact.json`).
