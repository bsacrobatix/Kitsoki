{"json":{"lessons":[{"category":"failure-patterns","description":"The parse error at column 2 mapped directly to an identifier suffix quantifier requiring at least one additional character, making the grammar rule itself the primary failure point rather than later OData comparison handling.","phase":"reproducing","severity":"medium"},{"category":"test-coverage","description":"A focused regression test for `parse_str(\"x eq 1\")` was enough to prove the one-character identifier path and guard the exact boundary case that multi-character identifier tests missed.","phase":"validating","severity":"medium"},{"category":"validation-signals","description":"The reported `./...` command error came from an incompatible Go-style validation command; Cargo package build and test commands were the applicable evidence for this Rust workspace.","phase":"validating","severity":"low"}],"metrics":{"cycles":0},"summary_markdown":"The bug was in the PEG identifier rule in `libs/modkit-odata/src/odata_parse.rs`: it required an initial identifier character followed by one or more suffix characters, so a valid one-character field name such as `x` was rejected before the comparison could parse.\n\nThe fix changed the suffix portion of the identifier grammar to zero-or-more characters while preserving the existing first-character restriction. This allows `x eq 1` and still prevents identifiers from starting with a digit or other invalid character.\n\nCoverage was added in `libs/modkit-odata/src/odata_parse_tests.rs` with `single_character_identifier`, which parses `parse_str(\"x eq 1\")` and asserts an equality comparison between identifier `x` and numeric value `1`.\n\nValidation passed with `cargo build -p cf-modkit-odata --lib`, the focused `cargo test -p cf-modkit-odata --lib single_character_identifier`, and the full package lib test run `cargo test -p cf-modkit-odata --lib` with 95 passing tests. There were 0 refinement cycles. The only misleading signal was a Go-style `./...` command error, which was not applicable to this Cargo workspace and did not affect validation.\n\nFor a similar parser bug, the shortest reliable path is to inspect the grammar quantifier at the reported location, add a minimal regression test for the exact accepted syntax, and validate both focused and package-level Cargo tests.","summary_title":"bug6 fixed: single-character OData identifiers parse correctly"}}