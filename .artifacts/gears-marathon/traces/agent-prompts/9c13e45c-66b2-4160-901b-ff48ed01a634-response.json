{"json":{"affected_files":["libs/modkit-canonical-errors/src/error.rs"],"alternative_approaches":["Add public constructor helpers such as `CanonicalError::from_io_error` and `CanonicalError::from_json_error`, but that would not make `?` work because `?` requires `From`/`Into`.","Route serde errors through the public resource-error builder API, but that requires more machinery than the local `InvalidArgument::format` context and risks changing resource fields unnecessarily."],"confidence":0.93,"external_dependencies":[],"fix_description":"In `libs/modkit-canonical-errors/src/error.rs`, add two narrow `From` implementations near the existing `Display`/`std::error::Error` impls. Implement `From\u003cstd::io::Error\u003e for CanonicalError` by returning `CanonicalError::internal(err.to_string()).create()`, which produces the existing Internal canonical category with status 500/title `Internal`. Implement `From\u003cserde_json::Error\u003e for CanonicalError` by returning `CanonicalError::__invalid_argument(InvalidArgument::format(err.to_string()))` (or an equivalent local construction that preserves the serde message as `detail`), which produces status 400/title `Invalid Argument` and makes `detail()` equal to the serde_json error message. No new dependency is needed because `serde_json` is already a dependency of `cf-modkit-canonical-errors`.","reasoning":"The reproduction tests fail at compile time: direct `CanonicalError::from(std::io::Error)` and `CanonicalError::from(serde_json::Error)` report mismatched types, and `?` propagation reports that `From\u003cstd::io::Error\u003e` and `From\u003cserde_json::Error\u003e` are not implemented for `CanonicalError`. The target crate already has category construction logic in the same module: `CanonicalError::internal(...).create()` maps to the Internal variant, while `InvalidArgument::format(...)` and `CanonicalError::__invalid_argument(...)` produce an InvalidArgument variant whose default detail is the format message. Adding these two impls at the error type's definition site is the smallest change that satisfies both direct conversion and `?` propagation without changing builders, public enum variants, status/title accessors, or unrelated ModKit runtime behavior.","root_cause":"`CanonicalError` defines its enum, accessors, `Display`, and `std::error::Error` in `libs/modkit-canonical-errors/src/error.rs`, but the impl block ends at lines 553-559 with no `impl From\u003cstd::io::Error\u003e for CanonicalError` or `impl From\u003cserde_json::Error\u003e for CanonicalError`. Because Rust's `?` operator uses `From`/`Into` to convert the source error into the function's error type, `std::io::Error` and `serde_json::Error` cannot be propagated into `Result\u003c_, CanonicalError\u003e`, and direct `CanonicalError::from(err)` resolves only to the identity `From\u003cCanonicalError\u003e` impl from core.","summary_markdown":"## Bug\n`cf_modkit_errors::CanonicalError` cannot be created from `std::io::Error` or `serde_json::Error`, so both `CanonicalError::from(err)` and `?` propagation into `Result\u003c_, CanonicalError\u003e` fail to compile.\n\n## Cause\n`libs/modkit-canonical-errors/src/error.rs` implements `Display` and `std::error::Error` for `CanonicalError`, but it does not implement `From\u003cstd::io::Error\u003e` or `From\u003cserde_json::Error\u003e`. Rust therefore has no conversion path for direct `from` calls or for the implicit conversion used by `?`.\n\n## Fix\nAdd two local `From` impls in `libs/modkit-canonical-errors/src/error.rs`:\n\n- `std::io::Error` -\u003e `CanonicalError::internal(err.to_string()).create()` for 500 / `Internal`.\n- `serde_json::Error` -\u003e invalid-argument canonical error using `InvalidArgument::format(err.to_string())`, preserving the serde message as `detail()`, for 400 / `Invalid Argument`.\n\nNo dependency changes are required; `serde_json` is already in the crate dependencies.\n\n## Confidence\n0.93 — the focused test currently fails only because the two `From` impls are missing, and the crate already has the exact category constructors needed for the expected behavior.","summary_title":"Add common error conversions for CanonicalError"}}