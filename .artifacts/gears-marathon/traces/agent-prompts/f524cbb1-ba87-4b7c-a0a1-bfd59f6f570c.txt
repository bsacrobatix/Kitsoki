# Closing the bug-fix run — produce the done artifact

You are wrapping up the run for **bug5** —
*bug5 — resource-group wrongly forces the RG type-registry prefix onto allowed-membership type codes. In cyberware-resource-group, TypeService::create_type / update_type validate each allowed_membership_types entry with validation::validate_type_code, which enforces the RG namespace prefix (gts.cf.core.rg.type.v1~). But membership resource types are EXTERNAL domain types (e.g. gts.cf.core.idp.user.v1~) per DESIGN.md and ADR-001 Type Map; they must NOT be forced into the RG type-registry namespace. Effect: callers must wrap external types in the RG prefix purely to pass validation — a contract divergence. Expected: allowed-membership type codes accept external (non-RG-prefixed) domain types, while still rejecting empty and over-length (>1024) codes; RG-prefixed codes remain accepted for backwards-compat. add_membership/remove_membership are already correct and must stay unchanged. Source: modules/system/resource-group/resource-group/src/domain/ (validation.rs, type_service.rs). Tests: cargo test -p cyberware-resource-group --test domain_unit_test.* after **0** refinement cycle(s).

The validation produced:

> Validated the applied resource-group membership type-code fix in `/Users/brad/code/gears-rust/.worktrees/marathon-bug5-gpt`.

Evidence:
- Implementation inspection: `validate_membership_type_code` trims/lowercases for validation and rejects empty or >1024-character codes without enforcing `gts.cf.core.rg.type.v1~`; `TypeService::create_type` and `TypeService::update_type` use it only for `allowed_membership_types`. RG type codes and parent type codes still use `validate_type_code` with the RG prefix requirement.
- `cargo build -p cyberware-resource-group` passed.
- `cargo test -p cyberware-resource-group --test domain_unit_test` passed: 81 passed, 0 failed.
- `cargo test -p cyberware-resource-group --test type_service_test` passed: 49 passed, 0 failed. This includes create/update accepting external membership types, create rejecting empty membership entries, update rejecting over-length membership entries, and existing membership replacement coverage.
- Focused acceptance filter `cargo test -p cyberware-resource-group --test type_service_test external_allowed_membership_type` passed: 2 passed, 0 failed.
- Final `git status --short` was clean.

The earlier `pattern ./...: directory prefix . does not contain main module or its selected dependencies` output is an invalid command/pattern issue for this Cargo workspace, not a product regression. The correct package-scoped build and test commands pass.



## Constraints

- `lessons` must be drawn from this run's actual evidence — not generic
  best-practice. At minimum one lesson per non-trivial cycle.
- Each lesson cites a category (e.g. `api-patterns`, `failure-patterns`,
  `judge-misclassification`) and a severity.
- `summary_markdown` is the postmortem: what the bug was, what the fix
  did, what cost the cycles (if any), and what changed about how we'd
  approach a similar bug next time.

## Output

Submit a `done_artifact` (see `schemas/done_artifact.json`).
