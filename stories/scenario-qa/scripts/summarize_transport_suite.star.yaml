inputs:
  suite:        { type: object, required: true }
  scenario_ref: { type: string, required: false }
  transport:    { type: string, required: false }

outputs:
  suite_summary: { type: string }
  suite_plan:    { type: string }
