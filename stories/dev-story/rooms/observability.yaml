# observability — STUB. Wave 3 / Phase 6 wires log + metric queries
# against a real observability provider.

states:

  observability:
    description: "Observability — logs / metrics (Wave 3 stub)."
    view:
      extends: "base"
      blocks:
        body:
          - prose: "Observability (Wave 3 stub)."
        choices:
          - heading: "Actions"
          - list:
              items:
                - label: "go_back"
                - label: "go_main"
                - label: "look"
    relevant_world: []
    on:
      go_back:
        - target: "{{ world.prev_state }}"
      go_main:
        - target: main
      look:
        - target: .
