# incident — STUB. Wave 3 imports `stories/incident/` (or whatever
# the on-call story is called).

states:

  incident:
    description: "Incident — production triage (Wave 3 stub)."
    view:
      extends: "base"
      blocks:
        body:
          - prose: "Incident (Wave 3 stub)."
        choices:
          - heading: "Actions"
          - list:
              items:
                - label: "go_back"
                - label: "go_main"
                - label: "look"
    relevant_world: []
    on:
      go_back:
        - target: "{{ world.prev_state }}"
      go_main:
        - target: main
      look:
        - target: .
