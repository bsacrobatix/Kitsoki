# deploy — STUB. Wave 3 / Phase 6 (proposal §3.x) wires the
# `deploy` sub-story with real iface.{vcs,ci}.* dispatch.

states:

  deploy:
    description: "Deploy — ship changes (Wave 3 stub)."
    view:
      extends: "base"
      blocks:
        body:
          - prose: "Deploy (Wave 3 stub)."
        choices:
          - heading: "Actions"
          - list:
              items:
                - label: "go_back"
                - label: "go_main"
                - label: "look"
    relevant_world: []
    on:
      go_back:
        - target: "{{ world.prev_state }}"
      go_main:
        - target: main
      look:
        - target: .
