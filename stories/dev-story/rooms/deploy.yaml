# deploy — STUB. Wave 3 / Phase 6 (proposal §3.x) wires the
# `deploy` sub-story with real iface.{vcs,ci}.* dispatch.

states:

  deploy:
    description: "Deploy — ship changes (Wave 3 stub)."
    view: |
      Deploy (Wave 3 stub).

      Intents: go_back, go_main.
    relevant_world: []
    on:
      go_back:
        - target: "{{ world.prev_state }}"
      go_main:
        - target: main
      look:
        - target: .
