Repair the delivery failure described by `{{ payload }}`.

Preserve or strengthen every test and gate. Run the configured deterministic
gate, commit the minimal fix, and create the retained Git bundle at the
deployment-provided bundle destination. Return its relative reference, SHA-256
digest, kind, and the trace reference. Do not push or mutate a protected ref.
