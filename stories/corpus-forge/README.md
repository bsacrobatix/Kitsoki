# Corpus forge

Selects a deterministic, frozen calibration corpus from any Arena-compatible
manifest, then rejects rows without an oracle and independent RED/GREEN proof.
It is no-LLM by design. Use a distinct heldout corpus for promotion claims.
